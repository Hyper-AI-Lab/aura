fastapi>=0.115.0
uvicorn[standard]>=0.32.0
httpx>=0.27.0
pydantic>=2.7.0
beautifulsoup4>=4.12.0
lxml>=5.0.0
# Scrapling fetch deps (used when heavy package installed)
curl_cffi>=0.7.0
browserforge>=1.0.0
# Optional heavy backends — installed when available; adapters degrade gracefully.
# crawl4ai, scrapling, scrapegraphai, browser-use
